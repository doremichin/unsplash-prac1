import React from 'react';
import styled from 'styled-components';

const Nav = () => (
  <Container>
    Nav
  </Container>
);

const Container = styled.div`
  margin-left: 40px;
`;

export default Nav;
